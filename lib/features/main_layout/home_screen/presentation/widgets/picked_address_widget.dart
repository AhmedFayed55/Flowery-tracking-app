import 'package:flutter/material.dart';

class PickedAddressWidget extends StatelessWidget {
  const PickedAddressWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        
      ],
    );
  }
}
