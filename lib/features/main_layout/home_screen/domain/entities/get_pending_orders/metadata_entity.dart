class MetadataEntity {
  MetadataEntity({
    this.currentPage,
    this.totalPages,
    this.totalItems,
    this.limit,
  });

  num? currentPage;
  num? totalPages;
  num? totalItems;
  num? limit;
}
